/**
 * Services Index
 *
 * Re-exports all service modules for easy importing.
 *
 * @example
 * ```ts
 * import { api, ApiError, withApiResponse } from "@/services";
 * ```
 */

export { api, ApiError, withApiResponse } from "./api";

